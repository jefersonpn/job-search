const numbers = [1, 2, 3, 4, 5]
const names = ['JEFERSON', 'WELLEN', 'CALEB', 'BENJAMIN']

const squares = numbers.map((number) => number * number)

const lowercaseNames = names.map((name) => name.toLowerCase())

console.log(lowercaseNames)
